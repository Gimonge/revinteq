# Revinteq v3 — Environment Variables
# Copy to .env and fill in real values. Never commit .env to Git.

# ── Django ────────────────────────────────────────────────────
DJANGO_SECRET_KEY=your-very-long-random-secret-key-here
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=yourdomain.co.ke,www.yourdomain.co.ke,YOUR_SERVER_IP

# ── Database ──────────────────────────────────────────────────
DB_NAME=revinteq
DB_USER=revinteq_user
DB_PASSWORD=your_mysql_password
DB_HOST=localhost
DB_PORT=3306

# ── Redis ─────────────────────────────────────────────────────
REDIS_URL=redis://127.0.0.1:6379/0
CELERY_BROKER_URL=redis://127.0.0.1:6379/0
CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/0

# ── Encryption (for Meta tokens, M-Pesa keys, AT keys) ────────
FIELD_ENCRYPTION_KEY=your-32-byte-base64-encryption-key

# ── Meta (Facebook + Instagram + WhatsApp) ────────────────────
META_APP_ID=your_meta_app_id
META_APP_SECRET=your_meta_app_secret
META_REDIRECT_URI=https://yourdomain.co.ke/api/v1/meta/callback/
META_SCOPES=ads_read,business_management,instagram_basic,instagram_manage_insights,whatsapp_business_management,whatsapp_business_messaging

# WhatsApp webhook verify token — set this same value in Meta App Dashboard
# under WhatsApp → Configuration → Webhook → Verify Token
WHATSAPP_WEBHOOK_VERIFY_TOKEN=choose_any_random_string_here

# ── Kommo CRM (one integration registered on Gimsc's Kommo dev account) ─
KOMMO_CLIENT_ID=your_kommo_integration_id
KOMMO_CLIENT_SECRET=your_kommo_secret_key
KOMMO_REDIRECT_URI=https://yourdomain.co.ke/api/v1/kommo/callback/

# ── Site ──────────────────────────────────────────────────────
SITE_URL=https://yourdomain.co.ke

# ── CORS ──────────────────────────────────────────────────────
CORS_ALLOWED_ORIGINS=https://yourdomain.co.ke,http://localhost:5173

# ── Email (optional — for invitation emails) ──────────────────
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your@email.com
EMAIL_HOST_PASSWORD=your_app_password

# ── NOTE: M-Pesa and Africa's Talking credentials ─────────────
# These are stored per-tenant in the database (encrypted).
# They are NOT set here — admin configures them per client
# via the admin portal:
#   Admin → Tenants → [Client] → M-Pesa Config
#   Admin → Tenants → [Client] → SMS Config
